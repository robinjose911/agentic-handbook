[pytest]
# Default validator collection. The runnable Python examples (examples/python/*) ship their own
# suites and are invoked explicitly, so they are not collected here.
testpaths = tools/validate
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
