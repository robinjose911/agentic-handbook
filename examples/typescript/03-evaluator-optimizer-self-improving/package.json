{
  "name": "@agentic-handbook/example-03-evaluator-optimizer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reference example 03: evaluator-optimizer self-improving loop (generate -> critique-against-rubric -> revise) against the mock provider.",
  "scripts": {
    "run": "node --experimental-strip-types run.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "zod": "^3.24.0"
  }
}
